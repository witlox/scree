apiVersion: v1
kind: Service
metadata:
  name: {{ include "scree.fullname" . }}
  labels:
    {{- include "scree.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "scree.selectorLabels" . | nindent 4 }}
